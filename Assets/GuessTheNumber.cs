using UnityEngine;

public class GuessTheNumber : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

    void GameSetup()
    {
        // Choose the random number that the player must guess
        // Update the header text to let the player know how many attempts are remaining
        // Clear the text in the input field
    }

    void SubmitGuess()
    {
        // Check if the number in the input field is correct
        // Update header text to let them know they are wrong
        // Decrease remaining attempts
        // Update header text to let them know they have won
    }
}
